# Inicializa una lista vacía para almacenar los números que cumplen con las condiciones
numeros_divisibles = []

# Itera a través del rango de 1500 a 2700 (ambos incluidos)
for numero in range(1500, 2701):
    # Verifica si el número es divisible por 7 y múltiplo de 5
    if numero % 7 == 0 and numero % 5 == 0:
        # Si cumple las condiciones, agrégalo a la lista
        numeros_divisibles.append(numero)

# Imprime los números que cumplen con las condiciones
print("Números divisibles por 7 y múltiplos de 5 en el rango de 1500 a 2700:")
print(numeros_divisibles)